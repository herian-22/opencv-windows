# C++ Vision OOP

Modular camera handling using C++ OOP and OpenCV. Optimized for Windows with MSVC, CMake, and vcpkg.

## 🛠 Prerequisites
- **Visual Studio 2022** (Desktop C++)
- **CMake** & **vcpkg**
- **OpenCV**: `vcpkg install opencv:x64-windows`

## 📂 Structure
- `include/`: Header files (`.hpp`)
- `src/`: Source files (`.cpp`)
- `build.bat`: One-click automation script

## 🚀 Quick Start
Run the automation script to Clean, Build, and Run:
```powershell
.\build.bat
